# SemarSIEM Configuration

# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=8080

# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=semarsiem
DB_USER=postgres
DB_PASSWORD=postgres
DB_SSL_MODE=disable

# Collector Configuration
COLLECTOR_UDP_PORT=514
COLLECTOR_TCP_PORT=601

# Development Notes:
# - Copy this file to .env and update the values as needed
# - For local development, use the values above
# - For Docker Compose, the DB_HOST should match the postgres service name